# api_debugger_example

Example app for the `api_debugger` package.

## Run

```bash
flutter run
```

Use the debugger switch in the app bar, then tap **Send sample request** to
capture and inspect a Dio request.

## Android

The Android platform project is included under `android/`.
